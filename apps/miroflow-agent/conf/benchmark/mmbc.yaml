# conf/benchmark/hle-text-500.yaml
defaults:
  - default
  - _self_

name: "mmbc"

data:
  data_dir: "../../data/MMBC"
  metadata_file: "standardized_data_shuffled_multi_image.jsonl"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1